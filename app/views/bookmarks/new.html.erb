<div class="w-50 d-flex flex-column align-items-center">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :movie_id , collection: @movie_titles%>
    <%= f.input :comment %>
    <%= f.button :submit, class: "btn btn-primary w-100" %>
  <% end %>
</div>
